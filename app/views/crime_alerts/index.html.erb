<h1>Listing crime_alerts</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @call_boxes.each do |call_box| %>
  <tr>
    <td><%= crime_alert.name %></td>
    <td><%= crime_alert.latitude %></td>
    <td><%= crime_alert.longitude %></td>
    <td><%= link_to 'Show', call_box %></td>
    <td><%= link_to 'Edit', edit_call_box_path(call_box) %></td>
    <td><%= link_to 'Destroy', call_box, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Call box', new_call_box_path %>
